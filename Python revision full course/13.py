# creating tuple using ()
t = (1, 2, 3, 4, 5)
t1 = ()
print(t1)

t2 = (1)  # WRONG way of writing a tuple with one element
t2 = (1,) # CORRECT way of writing a tuple with one element

# printing elements of tuple
print(t[0])

#cannot update the values of tuple
# t[0] = 34    #will return false values
